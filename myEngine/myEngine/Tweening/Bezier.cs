using System;
using System.Collections.Generic;
using System.Text;

namespace myEngine
{
    public class Bezier
    {
        /*
        //FIELDS
        float x1, y1, x2, y2;

        //CONTRUTOR
        public Bezier(float x1, float y1, float x2, float y2)
        {
            this.x1 = x1;
            this.y1 = y1;

            this.x2 = x2;
            this.y2 = y2;
        }

        //METHODS
        public float Get(float timePasted)
        {
            if (this.x1 == this.y1 && this.x2 == this.y2)
                return timePasted; // linear
            return this.CalcBezier(GetTForX(timePasted), y1, y2);
        }

        private float CalcBezier(aT, aA1, aA2)
        {
            return ((this.A(aA1, aA2) * aT + this.B(aA1, aA2)) * aT + this.C(aA1)) * aT;
        }

        private float GetTForX(float timePasted)
        {
            float guessT = timePasted;
            for(var i = 0; i < 4; i++)
            {
                float currentSlope = this.GetSlope(guessT, this.x1,)
            }
            return 0f;
        }
        */
    }
}
